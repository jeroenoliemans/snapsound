define(function () {
    //Do setup work here
    var controls = document.getElementById('controlPanel'),
    uploader = document.getElementById('uploader');

    return {
        controls: controls,
        uploader: uploader
    }
});